const chai = require('chai');
const expect = chai.expect;

const { Task } = require('./Task');

describe('task tests', () => {
    describe('constructor', () => {
        it('should not crash if given bad input parameters', () => {
            
        });
        it('should only allow the expected types as inputs', () => {

        });
        it('should throw an error if the date format is incorrect', () => {

        });
    });

    describe('update', () => {
        it('should not crash on bad inputs', () => {

        });
        it('should correctly update the appropriate fields',() => {

        });
    });
});